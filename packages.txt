# Pacotes que esta configuração precisa (Arch Linux, instalados com paru).
#
# Uma linha por pacote. Comentários com '#'. Linhas em branco são ignoradas.
# Aqui está só o que algum arquivo do repositório realmente chama: config,
# script de packages/bin, keybind do Hyprland, módulo do waybar ou tema.
# O resto do que está instalado na máquina de referência vive em
# packages-extra.txt (apps, toolchains, hardware) — veja o --extra
# do install.sh.
#
# Os marcados com "AUR" não estão nos repositórios oficiais.
#
# Pacotes que entram sozinhos como dependência (wireplumber, polkit, glib2,
# python, nodejs…) não são listados de propósito: o paru resolve.

# ── Base ──────────────────────────────────────────────────────────────────────
git                          # install.sh, tmux-sessionizer
stow                         # dots link/migrate
base-devel                   # makepkg, para compilar do AUR
paru                         # AUR (o install.sh compila sozinho se faltar)

# ── Hyprland e sessão ─────────────────────────────────────────────────────────
hyprland
hyprpaper                    # exec-once + troca de wallpaper no rofi-script
hypridle
hyprlock
hyprsunset
hyprpicker
hyprpolkitagent
uwsm                         # todo keybind lança via 'uwsm app --'
xdg-desktop-portal-hyprland
xorg-xwayland                # apps X sob o Hyprland

# ── Barra / shell gráfico ─────────────────────────────────────────────────────
quickshell                   # AUR — 'qs ipc call bar cycle'
waybar
libnotify                    # notify-send no window-info e rofi-script

# ── Menu ──────────────────────────────────────────────────────────────────────
rofi
rofimoji

# ── Screenshot / clipboard / input ────────────────────────────────────────────
grim                         # rofi-script --screenshot, printAndEdit
slurp
satty
wl-clipboard                 # wl-copy/wl-paste (cliphist, window-info)
cliphist
wev
keyd
brightnessctl

# ── Aparência ─────────────────────────────────────────────────────────────────
gnome-themes-extra           # temas GTK que o 'theme' aplica via gsettings
nwg-look
qt5-quickcontrols2
qt6ct
rose-pine-cursor             # AUR

# ── Fontes ────────────────────────────────────────────────────────────────────
noto-fonts
noto-fonts-cjk
noto-fonts-emoji
ttf-dejavu
otf-font-awesome             # ícones do waybar e do quickshell
woff2-font-awesome

# ── Terminal ──────────────────────────────────────────────────────────────────
ghostty                      # terminal dos on-click do waybar

# ── Shell e utilitários de linha de comando ───────────────────────────────────
zsh
starship
zoxide
fzf
eza
bat
fd
ripgrep
jq                           # window-info, rofi-script
# yazi
tmux
viu                          # preview de wallpaper no rofi-script
pastel                       # testar-cor
fastfetch
btop                         # on-click do waybar
unzip

# ── Git ───────────────────────────────────────────────────────────────────────
lazygit                      # tmux-lazygit

# ── Editor ────────────────────────────────────────────────────────────────────
neovim
tree-sitter-cli
lua51                        # dependência do luarocks
luarocks

# ── Áudio ─────────────────────────────────────────────────────────────────────
pipewire
pipewire-pulse               # módulo 'pulseaudio' do waybar
wiremix                      # on-click do waybar

# ── Rede ──────────────────────────────────────────────────────────────────────
networkmanager               # habilitado pelo install.sh
network-manager-applet       # ícone na tray do waybar
nm-connection-editor         # aberto pelo rofi-script

# ── Energia ───────────────────────────────────────────────────────────────────
power-profiles-daemon        # módulo do waybar, habilitado pelo install.sh

# ── Aplicativos usados pelas configs ──────────────────────────────────────────
zen-browser-bin              # AUR — keybind e exec-once
zathura-pdf-mupdf            # puxa o zathura, que tem config em packages/
xdg-utils                    # xdg-open, usado pelo gx do oil e do yazi
gimp                         # printAndEdit
